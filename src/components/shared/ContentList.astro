<div class="content-list">
  <slot />
</div>
<style>
  .content-list {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .content-list :global(article) {
    padding: var(--space-4) 0;
    border-bottom: 1px solid var(--border-color);
  }

  .content-list :global(article:last-child) {
    border-bottom: none;
  }
</style>
