---
import { NAVIGATION } from "../../constants/navigation";
import { getEntry } from "astro:content";

const pathname = new URL(Astro.request.url).pathname;
const showFish = pathname !== "/";

const siteConfig = await getEntry("site", "home");
const githubUrl = siteConfig?.data.socialLinks.find(link => link.platform === "GitHub")?.url;

const isActive = (path: string) => {
  if (path === "/") return pathname === "/";
  return pathname.startsWith(path);
};
---

<nav>
  <div class="nav-links">
    {
      NAVIGATION.map(({ name, path }) => (
        <a href={path} class={isActive(path) ? "active" : ""}>
          {name}
        </a>
      ))
    }
  </div>
  {showFish && (
    <div class="nav-right">
      <a href={githubUrl} target="_blank" rel="noopener noreferrer">
        <img src="/images/companies/low/fish.jpg" alt="Fish" class="nav-fish" loading="lazy" decoding="async" fetchpriority="low" />
      </a>
    </div>
  )}
</nav>

<style>
  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
  }

  .nav-links {
    display: flex;
    gap: 1.5rem;
  }

  .nav-links a {
    color: var(--text-muted);
    text-decoration: none;
    font-size: var(--text-sm);
    transition: color var(--transition-base);
    position: relative;
  }

  a {
    color: var(--text-muted);
    text-decoration: none;
    font-size: var(--text-sm);
    transition: color var(--transition-base);
    position: relative;
  }

  a:hover {
    color: var(--color-text);
  }

  a.active {
    color: var(--color-text) !important;
  }

  a.active::after {
    content: "";
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 100%;
    height: 1px;
    background: var(--text-muted);
  }

  .nav-right {
    display: flex;
    align-items: center;
    margin-left: auto;
  }

  .nav-right a {
    cursor: pointer;
  }

  .nav-fish {
    position: fixed;
    top: 1rem;
    right: 1rem;
    width: 2rem;
    height: 2rem;
    object-fit: cover;
    border-radius: 50%;
    z-index: 1000;
  }
</style>
